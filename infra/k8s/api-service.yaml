apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: dropradar
spec:
  selector:
    app: dropradar
    component: api
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  type: ClusterIP
